{
    "name": "literaty",
    "version": "1.0.0",
    "scripts": {
      "build": "cd cliente/literaty && npm install && npm run build",
      "start": "cd servidor && node servidor.js",
      "deploy": "npm run build && npm run start"
    }
  }
  